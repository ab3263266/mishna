.venv/
__pycache__/
*.pyc
.env
.pytest_cache/

# Local SQLite development database (regenerated by app/db/seed.py).
# The glob must cover -wal and -shm too: WAL mode creates mishnah.db-wal and
# mishnah.db-shm alongside the database, and "*.db" does not match those.
*.db
*.db-wal
*.db-shm
