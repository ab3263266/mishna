# Core
# NOTE: SQLAlchemy < 2.0.43 fails to import on Python 3.14 (typing internals
# changed under it). Verified working on 3.14 with 2.0.52.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0

# Windows and slim Linux images ship no system tz database, and every day
# boundary in this app is computed with ZoneInfo. Without it the app raises
# ZoneInfoNotFoundError on the first request.
tzdata==2025.2

# Persistence
SQLAlchemy==2.0.52
psycopg[binary]==3.2.3
alembic==1.14.0

# Auth
google-auth==2.37.0
httpx==0.28.1
PyJWT==2.10.1

# Jewish calendar / zmanim.
# `zmanim` is a Python port of KosherJava (sunset, candle lighting, tzeis).
# `pyluach` handles Hebrew dates and festivals.
# Both are optional at runtime - services/zmanim.py falls back to fixed local
# clock times if they are not installed, so the app still boots without them.
zmanim==0.3.2
pyluach==2.2.0

# Dev
pytest==8.3.4
freezegun==1.5.1
